import { Metadata } from "next";

export const metadata: Metadata = {
  title: "فراموشی رمز عبور | فروشگاه قهفی",
};

export default function ForgotPasswordPage() {
  return <main>فراموشی رمز عبور</main>;
} 